<div class="row">
  <% @projects.each do |p| %>
  <div class="col-sm-6 mb-3">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title">
          <%=link_to(p.title, p ) %>
        </h5>
          <p class="card-text">
             <%= p.description%>
        </p>
        </div>
        </div>
        </div>
  <% end %>
</div>
